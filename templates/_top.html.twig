<div class="navbar-custom">
    <ul class="list-unstyled topbar-menu float-end mb-0">
        <li class="dropdown notification-list d-lg-none">
            <a class="nav-link dropdown-toggle arrow-none" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="false" aria-expanded="false">
                <i class="dripicons-search noti-icon"></i>
            </a>
            <div class="dropdown-menu dropdown-menu-animated dropdown-lg p-0">
                <form class="p-3">
                    <input type="text" class="form-control" placeholder="{{ 'rechercher'|trans|capitalize }} ..." aria-label="Recipient's username">
                </form>
            </div>
        </li>
        <li class="notification-list">
            <a class="right-panel-button end-bar-toggle" href="javascript: void(0);">
{#            <a class="right-panel-button nav-link end-bar-toggle" href="javascript: void(0);">#}
                <i class="dripicons-gear noti-icon"></i>
            </a>
        </li>

        <li class="dropdown notification-list">
            <a class="nav-link dropdown-toggle nav-user arrow-none me-0" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="false"
               aria-expanded="false">
                    <span class="account-user-avatar">
                    {% if app.user.upload.path is defined   %}
                        <img src="{{ asset(app.user.upload.path) }}" alt="user-image" class="rounded-circle">
                    {% else %}
                        <img src="{{ asset('build/users/profil.png') }}" alt="user-image" class="rounded-circle">
                    {% endif %}
                    </span>
                <span>
                    {# <span class="account-position">{{ niceDisplayUserName(app.user.firstName,app.user.lastname)['fullname_a'] }}</span> #}
                    {# <span class="account-user-name">{{ app.user.email }}</span> #}
                </span>
            </a>
            <div class="dropdown-menu dropdown-menu-end dropdown-menu-animated topbar-dropdown-menu profile-dropdown">
                <!-- item-->
                <div class=" dropdown-header noti-title">
                    <h6 class="text-overflow m-0">{{ 'bienvenue'|trans|capitalize }} !</h6>
                </div>
                {# <!-- item-->
                {% set space = app.request.pathInfo|split('/')['1'] is same as 'user' ? app.request.pathInfo|split('/')['4'] : app.request.pathInfo|split('/')['1']   %}
                <a href="{{path('user_show',{'space': space })}}" class="dropdown-item notify-item">
                    <i class="mdi mdi-account-circle me-1"></i>
                    <span>{{ 'mon compte'|trans|capitalize }}</span>
                </a>
                <!-- item-->
                <a href="{{ path('app_logout') }}" class="dropdown-item notify-item">
                    <i class="mdi mdi-logout me-1"></i>
                    <span>{{ 'se déconnecter'|trans|capitalize }}</span>
                </a> #}
            </div>
        </li>
    </ul>
    <button class="button-menu-mobile open-left">
        <i class="mdi mdi-menu"></i>
    </button>
    {% if add_search is same as (true) %}
        <div class="app-search dropdown d-none d-lg-block">
            <form>
                <div class="input-group">
                    <input type="text" class="form-control dropdown-toggle"  placeholder="{{ 'rechercher'|trans|capitalize }}..." id="top-search-terrain">
                    <span class="mdi mdi-magnify search-icon"></span>
                    <button class="input-group-text btn-primary" id="button-search-terrain" type="submit">{{ 'rechercher'|trans|capitalize}}</button>
                </div>
            </form>

            <div class="dropdown-menu dropdown-menu-animated dropdown-lg" id="search-dropdown">
                <!-- item-->
                <div class="dropdown-header noti-title">
                    <h5 class="text-overflow mb-2">17 <span class="text-danger">{{ 'résultats'|trans }}</span> {{ 'trouvés'|trans }}</h5>
                </div>
            </div>
        </div>
    {% endif %}
</div>
